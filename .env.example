# AI模型配置
AI_API_KEY=your_a...n
# 数据库配置
DB_PASSWORD=your_d...EOF

# 2. 验证
cat /e/JavaProject/stock-agent-app/.env.example
echo "==="

# 3. 提交并强制推送
cd /e/JavaProject/stock-agent-app
git add .env.example
git commit -m "fix: 清除.env.example中的真实key"
git push -u origin main 2>&1
